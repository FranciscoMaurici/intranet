import styled from 'styled-components'

import FragmentText from '../FragmentText'

export const PageTitleStyled = styled(FragmentText).attrs({
  variant: 'headingRegular',
})`
  margin-bottom: 0.7035em;
`
