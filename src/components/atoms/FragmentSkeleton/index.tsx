import { Skeleton } from '@mui/material'

import { IProps } from './types'

const FragmentSkeleton = (props: IProps) => <Skeleton {...props} />

export default FragmentSkeleton
export type { IProps }
