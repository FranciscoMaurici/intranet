import { LinearProgressProps } from '@mui/material'

import { StyledLinearProgress } from './styles'

const FragmentLinearProgress = (props: LinearProgressProps) => (
  <StyledLinearProgress {...props} />
)

export default FragmentLinearProgress
