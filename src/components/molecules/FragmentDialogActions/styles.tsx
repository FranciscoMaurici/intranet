import MUIDialogActions from '@mui/material/DialogActions'
import styled from 'styled-components'

import { borders } from '@theme'

export const FragmentDialogActionsStyled = styled(MUIDialogActions)`
  border-top: ${borders.basicCard};
  height: 3.44em;
  padding-right: 1.5em;
`
